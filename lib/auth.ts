// Cookie names — managed exclusively by the server via Set-Cookie (httpOnly).
// The client never reads or writes these directly.
export const AUTH_COOKIE    = 'financeiro_token'
export const REFRESH_COOKIE = 'financeiro_refresh'
